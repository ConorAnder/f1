# Formula1 Weather Effects Graphic

To generate the csv files containing the data in this project, run the getData.py script.
Be sure to download the dependencies from pip.

```
pip install -r requirements.txt
python3 getData.py
```